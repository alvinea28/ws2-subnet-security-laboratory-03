# TODO: build azurerm_network_security_group.this first.
# Then add standalone rules with for_each = var.rules and subnet associations
# with for_each = var.subnet_ids. Never create the supplied subnet or RG.
